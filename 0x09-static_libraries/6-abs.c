#include "main.h"

/**
 * _abs - returns the absolute value of an integer
 *
 * @n: is an integer argument
 *
 * Return: Always 0.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
